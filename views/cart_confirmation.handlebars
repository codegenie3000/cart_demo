{{> header }}

<section id="checkout" class="step03">
<div class="container">
    <div class="row">
        <div class="col-sm-12 text-center">
            <h1>Checkout - Verify Information</h1>
        </div>
    </div>
	<div class="row">
		<div class="col-sm-6">
			<div class="row">
				<div class="col-sm-12">
					<div class="panel panel-default">
						<div class="panel-heading">Billing Information <a href="/cart/billing">edit</a></div>
						<div class="panel-body">
							{{#with billingAddress}}
								Name: {{name}}<br>
								Address: {{address}}<br>
								City, State, Zip: {{city}}, {{state}} {{zip}}<br>
								Phone: {{phone}}<br>
								Email: {{email}}
							{{/with}}
						</div>
					</div>
				</div>
				<div class="col-sm-12">
					<div class="panel panel-default">
						<div class="panel-heading">Shipping Information <a href="/cart/shipping">edit</a></div>
						<div class="panel-body">
							{{#with shippingAddress}}
								Name: {{name}}<br>
								Address: {{address}}<br>
								City, State, Zip: {{city}}, {{state}} {{zip}}<br>
								Phone: {{phone}}<br>
							{{/with}}
						</div>
					</div>
				</div>
			</div>
		</div>
		<div class="col-sm-6">
			<div class="panel panel-default">
				<div class="panel-heading">
					Cart <a href="/cart">edit</a>
				</div>
				<div class="panel-body">
					{{#each cartItem}}
						<div class="row">
							<div class="col-xs-6 text-left">{{title}} ({{qty}})</div>
							<div class="col-xs-6 text-right">{{lineTotal}}</div>
						</div>
					{{/each}}
					<hr>
					<div class="row">
						<p class="col-sm-12 text-right">Shipping: {{shipping}}</p>
					</div>
					<div class="row">
						<p class="col-sm-12 text-right">Total: {{total}}</p>
					</div>
				</div>
			</div>
		</div>
	</div>
	<div class="row">
		<div class="col-sm-12">
			<a href="/cart/billing" class="btn btn-default">Back</a>
			<a href="/cart/payment"><button class="btn btn-primary">Next</button></a>
		</div>
	</div>
</div>

{{> information_not_complete }}
{{> scripts }}
</section>

{{> footer }}